{
  "name": "tilcayocat",
  "version": "1.0.0",
  "private": true,
  "description": "Tilcayo Cat — a mobile-first mouse-catching game starring Leopardus tilcayo, the first new wild cat species described in over a century.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "og": "node scripts/og.mjs",
    "deploy": "npm run build && wrangler pages deploy dist --project-name tilcayocat"
  },
  "devDependencies": {
    "sharp": "^0.34.5",
    "typescript": "^5.9.3",
    "vite": "^7.3.6"
  }
}
